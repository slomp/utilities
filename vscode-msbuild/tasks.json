{
    "version":"2.0.0",
    "tasks": [
        {
            "label": "Build Solution (Solution.sln)",
            "command": "${workspaceFolder}/.vscode/build.bat",
            "args": [],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "type": "shell",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
